% Конфигурация для расчетов в экономике
(def revenue 50000);
(def expenses 30000);
(def employees 50);

% Прибыль
|revenue - expenses|

% Средний доход на сотрудника
|revenue / employees|

% Длина строки с названием компании
(def company_name "TechCorp");
|len(company_name)|
