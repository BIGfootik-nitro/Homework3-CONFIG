% Конфигурация для операций с текстом
(def greeting "Hello");
(def name "Alice");

% Длина строки
|len(greeting)|

% Код символа
|ord(greeting[0])|

% Сложение строк
|greeting + " " + name|

% Длина строки после сложения
|len(greeting + name)|
